export enum JoinType {
  'NONE',
  'OWNER',
  'PLAYER',
  'WATCHER',
}

export enum GameType {
  'NORMAL',
  'RANK',
}

export enum FriendReqType {
  'SEND',
  'RECEIVE',
  'ACCEPT',
}

export enum ChatType {
  'PUBLIC',
  'PRIVATE',
  'PASSWORD',
}
